import numpy as np

marks = np.array([88, 95, 76, 90, 82])

print("Student Marks")

print(marks)

print("\nAverage Marks:", np.mean(marks))

print("Highest Marks:", np.max(marks))

print("Lowest Marks:", np.min(marks))

print("Total Marks:", np.sum(marks))